#include <cstdint>

#include "header.h"
#include <math.h>

int ListCities() {
    City listofcities;
    for (int i = 0, i < listofcities, i++) {
        std::cout << listofcities << "\n";
    }
}